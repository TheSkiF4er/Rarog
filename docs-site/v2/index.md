# Rarog 2.x (legacy)

This section is reserved for legacy 2.x documentation.

The recommended way to access full 2.x docs is:

- use the `2.x` git tag/branch of this repository;
- run the docs site from that branch.

For new projects, use Rarog **3.x** and the main docs (v3).

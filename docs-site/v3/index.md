# Rarog 3.x Docs

Rarog 3.x is the current stable branch.

The main documentation lives at the root of the docs site:

- Russian (`/`),
- English (`/en/`).

The `/v3` path is a logical alias that can be pointed to the same docs tree
when deployed (for example, `docs.cajeer.ru/rarog/v3/...`).

# Showcase & UI Kits (EN)

Pointers to:

- **Rarog UI Admin** — admin dashboard kit;
- **Rarog Landing Kit** — landing sections;
- **Rarog SaaS Starter** — auth/dashboard/settings/billing layouts.

Each kit lives under `examples/ui-kits/*` and is wired to the same Rarog
CSS/JS Core as your main project.

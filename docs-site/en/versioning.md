# Versioning & Support (EN)

Rarog 3.x follows SemVer and defines:

- what constitutes breaking change,
- how long 3.x is supported,
- how 2.x is treated as legacy.

It also describes docs versioning:

- `/v3/...` — main docs for 3.x,
- `/v2/...` — legacy 2.x reference.

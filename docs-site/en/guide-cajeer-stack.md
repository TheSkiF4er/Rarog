# Cajeer Stack Guide (EN)

Short overview of using Rarog inside the Cajeer ecosystem:

- treat Rarog as the design-token + utility + component + JS core layer;
- plug it into CMS/admin/frontends built by Cajeer;
- reuse UI kits (Admin, Landing, SaaS) as canonical layouts for Cajeer apps.

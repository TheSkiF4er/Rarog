# API Reference

API-centric index for:

- CSS utilities (by category),
- components and their class names / modifiers,
- JS Core (constructors, methods, events),
- config and CLI options,
- Plugin API hooks.

Use this as a quick lookup when you already know what you’re looking for.
